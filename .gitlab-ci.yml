stages:
  - test
  

run tests:
  stage: test
  image: ruby:2.5.8-alpine
  script:
    # - apt update
    - gem install bundler
    # - gem install execjs
    - bundle install
    - rspec
  artifacts:
    reports:
      coverage_report:
        coverage_format: cobertura
        path: coverage/coverage.xml
  tags:
    - test-runner